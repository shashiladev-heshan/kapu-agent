node_modules/
.next/
out/
.env
.env.local
*.tsbuildinfo
next-env.d.ts
.DS_Store

# local tooling
.claude/
*.log

# Obsidian per-machine state (graph.json/app config stay shared)
brain/.obsidian/workspace*
brain/.obsidian/cache
brain/

# product-video pipeline — local-only, never ships (see demo-video/README.md)
demo-video/

# internal working docs — reverse-engineered endpoints + strategy; keep out of the PUBLIC repo
docs/IMPLEMENTATION-PLAN.md

# Google service-account keys — downloaded JSON, NEVER commit
red-alloy-*.json
*service-account*.json
gcp-*key*.json

# A/B voice clips from local probes
scratch/
